{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile and role information, including biometric credentials.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique user ID (Auth UID or generated for manual users)." },
        "email": { "type": "string", "format": "email", "description": "User's email address (optional for manual users)." },
        "displayName": { "type": "string", "description": "User's display name." },
        "role": { "type": "string", "enum": ["admin", "superviseur", "personnel", "stagiaire", "ouvrier"], "description": "The user's role in the system." },
        "dailyRate": { "type": "number", "description": "Daily pay rate for workers." }
      },
      "required": ["uid", "displayName", "role"]
    },
    "Attendance": {
      "title": "Attendance",
      "description": "Attendance record for a user on a specific date, including arrival and departure times.",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "The UID of the user whose attendance is being marked." },
        "date": { "type": "string", "format": "date", "description": "The date for which attendance is marked (YYYY-MM-DD)." },
        "status": { "type": "string", "enum": ["present", "absent"], "description": "The attendance status." },
        "markedBy": { "type": "string", "description": "The UID of the admin who marked the attendance, or 'self' if checked in by user." },
        "checkIn": { "type": "string", "format": "date-time", "description": "The time the user checked in." },
        "checkOut": { "type": "string", "format": "date-time", "description": "The time the user checked out." },
        "timestamp": { "type": "string", "format": "date-time", "description": "The exact time the record was created." }
      },
      "required": ["userId", "date", "status", "markedBy", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles and roles."
    },
    "/attendance/{attendanceId}": {
      "schema": "Attendance",
      "description": "Attendance records."
    }
  }
}
